#include <string>
#include <vector>
#include <list>
#include <iostream>
#include <assert.h>

#include "ChessBoard.h"

Piece ChessBoard::gameBoard[][8];
ChessBoard ChessBoard::instance;

ChessBoard.instance ChessBoard::getInstance()
{
	return 0;
}

ArrayList<string> ChessBoard::getMoveHistory(int prevMoves)
{
	return 0;
}

void ChessBoard::updateBoard(int* move)
{
}

boolean ChessBoard::swap(int* move)
{
	return 0;
}
